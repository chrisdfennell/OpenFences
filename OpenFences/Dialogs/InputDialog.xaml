<Window x:Class="OpenFences.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Width="440"
        SizeToContent="Height"
        MinHeight="230"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1B1B20"
        Foreground="#EDEDED"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- Buttons -->
        <SolidColorBrush x:Key="BtnBg"       Color="#2D3547"/>
        <SolidColorBrush x:Key="BtnBgHover"  Color="#38425A"/>
        <SolidColorBrush x:Key="BtnBgPress"  Color="#2A344C"/>
        <SolidColorBrush x:Key="BtnBorder"   Color="#55607A"/>
        <SolidColorBrush x:Key="BtnFg"       Color="#FFFFFF"/>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource BtnFg}"/>
            <Setter Property="Background" Value="{StaticResource BtnBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BtnBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource BtnBgHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource BtnBgPress}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#202533"/>
            <Setter Property="BorderBrush" Value="#3A4453"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
        </Style>
    </Window.Resources>

    <Border Padding="16" Background="#26262C" CornerRadius="12" BorderBrush="#3B3B46" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock x:Name="PromptText" Grid.Row="1" Foreground="#C9CFDA" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="2">
                <TextBox x:Name="InputBox" Margin="0,0,0,14" Height="38"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2,0,0">
                    <Button Content="Cancel" Width="110" Margin="0,0,8,0" Click="Cancel_Click"/>
                    <Button Content="OK" Width="110" IsDefault="True" Click="OK_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>